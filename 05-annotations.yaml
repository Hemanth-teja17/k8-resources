# apiVersion: v1
# kind: Pod
# metadata:
#   name: annotations
#   annotations:
#     imageregistry: "https://hub.docker.com/_/registry"
# spec:
#   containers:
#   - name: nginx
#     image: nginx

kind: Pod
apiVersion: v1
metadata:
  name: annotations
  annotations:
    imageregistry: "https://hub.docker.com/"
    buildURL: "https://jenkins.joindevops.com/expense/backend/build/67"
spec:
  containers:
  - name: nginx
    image: nginx
