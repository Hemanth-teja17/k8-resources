apiVersion: v1
kind: Service
metadata:
  name: nginx-np
spec:
  type: NodePort
  selector:
    project: expense
    moodule: backend
    environment: dev
  ports:
    - name: nginx-svc-ports
      protocol: TCP
      port: 80
      targetPort: 80